fastapi==0.115.0
uvicorn[standard]==0.30.6

# Bumped 2.9.0 -> 2.31.0 on 2026-08-16 to unblock google-genai, below.
# 2.9.0 pinned httpx<0.28; 2.31.0 widens to >=0.26,<0.29, which is what lets
# httpx 0.28.1 satisfy google-genai as well.
#
# Verified rather than assumed, because a bad resolve here took this service
# down for two months once already. On a real Python 3.11 venv against the live
# database: every engine module imports, and maybe_single-on-zero-rows,
# select+eq, in_, order+limit and a read of the new ad_insights_daily table all
# behave the same as on 2.9.0.
supabase==2.31.0

requests==2.32.3
apscheduler==3.10.4
python-dotenv==1.0.1
playwright==1.44.0
anthropic==0.95.0

# Decrypts the customer's Meta access token, which reveal encrypts before
# storing (reveal/lib/ads/meta/oauth.ts -> crypto.py). Without this the engine
# cannot read a token connected after 2026-08-16 and every Meta call is skipped
# as "not connected". Verified working on Python 3.9 through 3.13.
cryptography==46.0.3

# RESTORED 2026-08-16, after being omitted for two months.
#
# It pins httpx>=0.28.1, which was unsatisfiable against supabase 2.9.0's
# httpx<0.28. The supabase bump above resolves that: httpx 0.28.1 satisfies
# both. Confirmed by a real install, not a metadata read.
#
# This is the module that makes AI ad creatives. Without it
# creative_engine.GEMINI_AVAILABLE was False, so bootstrap published a campaign
# with no images and the autopilot could pause a losing ad but never build the
# replacement it had just queued for the customer to approve. Now True.
#
# ⚠️ Requires Python >= 3.10, which is why .python-version pins 3.11. Nixpacks
# picks its own default otherwise, and a default of 3.9 fails this resolve.
google-genai==2.18.1

Pillow==11.2.1
