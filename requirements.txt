fastapi==0.115.0
uvicorn[standard]==0.30.6
supabase==2.9.0
requests==2.32.3
apscheduler==3.10.4
python-dotenv==1.0.1
playwright==1.44.0
anthropic==0.95.0
# google-genai is intentionally omitted. It pins httpx>=0.28.1 while supabase
# 2.9.0 pins httpx<0.28, an unsatisfiable conflict that failed every build.
# It is only used by creative_engine.py for AI ad-creative image generation,
# which already guards its import (GEMINI_AVAILABLE = False on ImportError) and
# degrades gracefully, so the core campaign, rules, Meta and autopilot engines
# deploy without it. Re-add it behind a supabase bump when image generation is
# wanted, not before.
Pillow==11.2.1
